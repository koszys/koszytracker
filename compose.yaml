services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: koszy
      POSTGRES_PASSWORD: koszymoe
      POSTGRES_DB: wishtracker
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: